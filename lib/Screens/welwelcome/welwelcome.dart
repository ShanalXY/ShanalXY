import 'package:flutter/material.dart';
import 'package:divider/Screens/welwelcome/components/body.dart';

class welwelcome extends StatelessWidget {
  const welwelcome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Body(), //check this const later
    );
  }
}
